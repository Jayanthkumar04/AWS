IAM (IDENTITY ACCESS MANAGEMENT)

IAM user can be user or application(programmatically access application)

=====================
steps to create IAM user
======================

1)search for IAM

2)reach IAM console

==>If spring boot app wants to access some aws services then Iam is to be created to this also

3)user groups   users   roles  policies are shown

4)click on create user, provice user access to management console

5)provide username   (any thing)

6)user type  

    => if many users user Identity center

   => if less users user Iam user


7)autogenerated or custom password

8)next permissions

        =>copy permissions  (other users permissions ccan be copied)
       =>add user to group
      => attach policies directly
                   (here polices can be given full access or limited access)
                  (just search for service and select limited access)
                 

9)crerate a user


10)user groups are very use full to a particular group having many users access can be given

11)IAM Role

temporary permission to user to do a specific task

  I)create role
        (For aws account)
 ii)add permissions 
       (select what permissions u want to give  (billings...))

iii)give roleName  and crerate role

iv)assign role to user  

     add permissions(create inline policy)
     in policy editor 
  "Action":"sts:AssumeRole"
 "Resource":"Arn" available in role"

give policy name


if wanted to enable cost related to IAM user
in root account  activate IAM User and Role Access to Billing Information







